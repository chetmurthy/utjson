local type rec group = object && [
    "hosts": object && [ /[a-zA-Z.-_0-9]/ : null; ];
    "vars": object;
    "children": [ /[a-zA-Z-_0-9]/ : group; ];
];
in type nonrec t = object && [ "all": group; ];
end;

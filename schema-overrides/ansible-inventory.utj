module DEFINITIONS = struct
type rec group = object && [
    "hosts": (object || string) && [ /[a-zA-Z.-_0-9]/ : (object || null); ];
    "vars": object;
    "children": [ /[a-zA-Z-_0-9]/ : group; ];
];
end ;
open DEFINITIONS ;
type nonrec t = object && [ "all": group; ];
end;
